
  # Jhon Jojoa - Portafolio Interactivo

  Este es el código fuente del portafolio interactivo de Jhon Jojoa.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  